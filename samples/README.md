# Samples Directory

Place your aerial inspection images here before running the detector.

**Supported formats:** `.jpg`, `.jpeg`, `.png`, `.bmp`, `.tif`, `.tiff`

## Quick test

```bash
python src/crack_detector.py samples/your_image.jpg --output outputs/result.jpg --show
```
